use std::path::Path;

pub fn run(_book_path: &Path) -> Result<(), String> {
    Err(
        "The 'window' command will be implemented in Milestone 5 (Minimal winit/wgpu Window)"
            .to_string(),
    )
}
